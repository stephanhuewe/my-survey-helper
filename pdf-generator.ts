/**
 * Created by malaka on 11/12/17.
 */

export let test1 = (arg) => {

  console.log('hi');
  const columns = ['ID', 'Name', 'Country'];
  const rows = [
    [1, 'Shaw', 'Tanzania'],
    [2, 'Nelson', 'Kazakhstan'],
    [3, 'Garcia', 'Madagascar'],
  ];

// Only pt supported (not mm or in)
//   const doc = new jsPDF('p', 'pt');
//   doc.autoTable(columns, rows);
//   doc.save('table.pdf');
};
